server:
  port: 8080

spring:
  application:
    name: tiandi-bank-server

  datasource:
    url: jdbc:mysql://localhost:3306/tiandi_bank?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 250712
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.kris.tiandi.bank.pojo

logging:
  level:
    com.kris.tiandi.bank.mapper: debug

